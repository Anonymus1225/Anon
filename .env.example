# Gemini API Key Example

# To set up the Gemini API key, create a new file called `.env` in this directory and add your API key as follows:

```
GEMINI_API_KEY=your_api_key_here
```

# Make sure to replace `your_api_key_here` with your actual Gemini API key and keep the `.env` file private!